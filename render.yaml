services:
  - type: worker
    name: astro-telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    plan: starter
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: USER_TZ
        value: Europe/Moscow
